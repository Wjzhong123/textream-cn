# 📋 真实浏览器手动测试清单

**项目**: Textream 智能提词器
**时间**: 2026-07-25 09:50
**测试环境**:
- 前端: http://localhost:3000 (PID 2562)
- 后端: http://localhost:9123 (PID 8173)
- 浏览器: 默认浏览器

---

## 🎯 测试目标

验证 web-console 的所有核心功能是否能正常工作

---

## 📊 测试清单

### 1. 页面加载 ✅

- [ ] 页面成功加载（无白屏/错误）
- [ ] 显示"连接中..."或"已连接"状态
- [ ] 导航菜单显示正确（弹幕监控、记忆管理、知识库）
- [ ] 设置按钮可见

**预期结果**: 页面在 2 秒内加载完成，显示导航栏

---

### 2. 连接状态检查 ✅

- [ ] 健康检查通过（显示"已连接"）
- [ ] 每 5 秒自动刷新连接状态
- [ ] 连接失败时显示"服务未连接"

**预期结果**: 显示绿色"已连接"状态

---

### 3. 弹幕监控面板

#### 3.1 截图区域设置

- [ ] 点击"设置区域"按钮
- [ ] 弹出区域设置表单
- [ ] 输入坐标: x=100, y=100, width=600, height=400
- [ ] 点击"保存"按钮
- [ ] 提示"区域已设置"

**测试命令**:
```bash
curl -X POST http://localhost:9123/api/danmaku/region \
  -H "Content-Type: application/json" \
  -d '{"x":100,"y":100,"width":600,"height":400}'
```

#### 3.2 启动弹幕捕获

- [ ] 点击"启动捕获"按钮
- [ ] 状态变为"正在捕获"
- [ ] 开始显示 OCR 文本流
- [ ] 每 1-2 秒更新一次

**测试命令**:
```bash
curl -X POST http://localhost:9123/api/danmaku/start
```

#### 3.3 查看捕获状态

- [ ] 点击"查看状态"按钮
- [ ] 显示当前状态（running/running）
- [ ] 显示截图区域坐标
- [ ] 显示捕获到的文本数量

**测试命令**:
```bash
curl http://localhost:9123/api/danmaku/status
```

#### 3.4 停止捕获

- [ ] 点击"停止捕获"按钮
- [ ] 状态变为"已停止"
- [ ] 不再显示新文本

**测试命令**:
```bash
curl -X POST http://localhost:9123/api/danmaku/stop
```

---

### 4. 救场话术侧边栏

#### 4.1 话术档位切换

- [ ] 默认显示"简单"档位
- [ ] 点击"深度"档位，话术内容更新
- [ ] 点击"幽默"档位，话术内容更新
- [ ] 切换流畅，无延迟

#### 4.2 一键复制

- [ ] 点击话术卡片的"复制"按钮
- [ ] 提示"已复制到剪贴板"
- [ ] 粘贴到文本编辑器验证内容正确

---

### 5. 记忆管理界面

#### 5.1 记忆列表

- [ ] 显示记忆列表（如果为空，显示"暂无记忆"）
- [ ] 每条记忆显示：标题、内容、标签、时间
- [ ] 支持分页（每页 10 条）

**测试命令**:
```bash
curl http://localhost:9123/api/memory/list?limit=10
```

#### 5.2 添加记忆

- [ ] 点击"添加记忆"按钮
- [ ] 填写表单：标题、内容、标签
- [ ] 点击"保存"
- [ ] 新记忆出现在列表顶部

**测试命令**:
```bash
curl -X POST http://localhost:9123/api/memory/add \
  -H "Content-Type: application/json" \
  -d '{"title":"测试记忆","content":"这是一个测试","tags":["test"],"importance":3}'
```

#### 5.3 搜索记忆

- [ ] 在搜索框输入关键词
- [ ] 显示匹配的记忆列表
- [ ] 支持模糊搜索

**测试命令**:
```bash
curl "http://localhost:9123/api/memory/search?q=测试"
```

#### 5.4 删除记忆

- [ ] 点击记忆的"删除"按钮
- [ ] 确认提示
- [ ] 记忆从列表移除

---

### 6. 知识库管理

#### 6.1 知识库列表

- [ ] 显示知识库条目列表
- [ ] 显示条目名称、内容摘要、创建时间

**测试命令**:
```bash
curl http://localhost:9123/api/knowledge/list
```

#### 6.2 搜索知识库

- [ ] 在搜索框输入关键词
- [ ] 显示匹配的知识库条目
- [ ] 支持全文搜索

**测试命令**:
```bash
curl "http://localhost:9123/api/knowledge/search?q=提词器"
```

---

### 7. 设置菜单

#### 7.1 服务器配置

- [ ] 点击设置按钮
- [ ] 显示配置表单
- [ ] 服务器地址: http://localhost:9123
- [ ] LLM 提供商: SiliconFlow
- [ ] 可修改并保存

#### 7.2 配置持久化

- [ ] 修改配置后刷新页面
- [ ] 配置保持（从 localStorage 加载）

---

### 8. 响应式设计

- [ ] 在不同窗口大小下布局正常
- [ ] 移动端视图适配（可选）

---

### 9. 错误处理

#### 9.1 后端未启动

- [ ] 停止后端服务
- [ ] 刷新前端页面
- [ ] 显示"服务未连接"
- [ ] 不显示错误堆栈

#### 9.2 API 错误

- [ ] 输入错误的 API 地址
- [ ] 显示连接失败提示
- [ ] 不崩溃

---

### 10. 性能检查

- [ ] 页面加载时间 < 2 秒
- [ ] API 响应时间 < 500ms
- [ ] 滚动流畅（60fps）
- [ ] 内存占用 < 100MB

---

## 🐛 问题记录

### 问题 1: [待填写]

**复现步骤**:
1. ...
2. ...

**预期行为**:
- ...

**实际行为**:
- ...

**截图**: [附件]

---

### 问题 2: [待填写]

...

---

## ✅ 测试结论

**通过**: ___/10 个功能模块
**失败**: ___/10 个功能模块
**阻塞问题**: [列出严重问题]

**建议**:
- [ ] 可进入下一阶段（WebSocket 集成测试）
- [ ] 需要修复 [___] 个问题后重测
- [ ] 需要优化 [___] 个体验问题

---

## 📝 测试日志

**测试开始时间**: 2026-07-25 09:50
**测试人员**: Claude Code
**浏览器**: [待填写]
**操作系统**: macOS

### 测试过程记录

| 时间 | 测试项 | 结果 | 备注 |
|------|--------|------|------|
| 09:50 | 页面加载 | ⏳ | 待测试 |
| 09:51 | 连接状态 | ⏳ | 待测试 |
| 09:52 | 弹幕监控 | ⏳ | 待测试 |
| 09:53 | 救场话术 | ⏳ | 待测试 |
| 09:54 | 记忆管理 | ⏳ | 待测试 |
| 09:55 | 知识库 | ⏳ | 待测试 |
| 09:56 | 设置菜单 | ⏳ | 待测试 |
| 09:57 | 响应式设计 | ⏳ | 待测试 |
| 09:58 | 错误处理 | ⏳ | 待测试 |
| 09:59 | 性能检查 | ⏳ | 待测试 |

---

**测试完成时间**: [待填写]
**测试人员签字**: Claude Code

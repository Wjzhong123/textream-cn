# Textream Agent Core 依赖
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6
openai>=1.0.0
anthropic>=0.8.0
httpx>=0.25.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# 弹幕捕获依赖（极简方案）
Pillow>=10.0.0           # ImageGrab 屏幕截图
pytesseract>=0.3.0       # OCR 文本识别
numpy>=1.24.0            # 图像预处理（配合 cv2）

# CaptiOCR 附加依赖
keyboard>=0.13.5             # 全局热键注册（macOS 需要 pyobjc）

# 注意：不需要 mss 或 opencv-python（已由 CaptiOCR 处理）
# - Pillow.ImageGrab 替代 mss（零额外依赖）
# - cv2 仅用于图像预处理（可选，可用 PIL 替代）

# 可选：Windows OCR（比 Tesseract 更快）
# rapidocr-onnxruntime>=1.3.0
